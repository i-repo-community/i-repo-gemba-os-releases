---
title: ユーザーマニュアル
nav_order: 2
has_children: true
---

# i-Repo GEMBA OS ユーザーマニュアル

**i-Repo GEMBA OS** は、i-Reporter で集めた現場の帳票データを、そのままにせず「使える形」にするためのデスクトップアプリです（Windows / macOS）。むずかしい設定やプログラミングは要りません。

できることは、大きく3つです。

| やりたいこと | このアプリでの言い方 | ざっくり言うと |
|---|---|---|
| 帳票データを社内のデータ置き場へ自動で送りたい | **配信（フロー）** | 「どの帳票を・いつ・どこへ」を決めておくと、自動で送り続けます |
| 送ったデータが本当に入ったか確認したい | **帳票ライブビュー / 現場蓄積データ** | 送信元と送信先を突き合わせ、中身もアプリ内で見られます |
| データについて人に聞くように質問したい | **現場 AI チャット** | 「先月の不具合報告を要約して」「月別の件数は？」のように、要約も**集計**も AI に日本語で聞けます |

送り先（データ置き場）は、お客様の環境に合わせて選べます — **SQLite / Parquet / MongoDB / Elasticsearch / BigQuery / Amazon S3 / Google Cloud Storage / Azure Blob / ファイル共有** など。配信済みを確認した古い帳票は、削除プラグインで安全に整理できます。

<figure class="screenshot">
  <img src="../assets/img/screen-overview.png" alt="i-Repo GEMBA OS のメイン画面（概要ダッシュボード）">
  <figcaption>メイン画面（概要）— 全体の状況がひと目で分かります</figcaption>
</figure>

## このマニュアルの構成

左のメニュー（このページの下にぶら下がる項目）から、知りたいところへ進めます。

1. [初回セットアップ](manual-setup.html) — 接続設定と、送り先プラグインの準備
2. [アプリの更新（バージョンアップ）](manual-update.html) — 新しい版への入れ替え方
3. [画面の使い方](manual-screens.html) — 各画面の操作（概要・フロー作成・帳票ライブビュー・API・スケジュール・現場 AI チャット など全15画面）
4. [常駐（システムトレイ）](manual-tray.html) — 閉じても裏で動かす
5. [着信 Webhook の詳細](manual-webhook.html) — 外部システムからフローを起動する
6. [AI 連携の詳細](manual-ai.html) — Claude / Codex などのエージェント連携
7. [トラブルシューティング](manual-troubleshooting.html) — 困ったときに
8. [用語集](manual-glossary.html) — 用語の補足

> はじめての方は、まず概要画面の **「はじめかたチェックリスト」**（各手順を実画面へ案内）か、どの画面でも右上の
> **「操作をAIに聞く」** から始めるのが近道です。じっくり読むなら **初回セットアップ → 画面の使い方（フロー作成）** の順がおすすめです。

## 読み取り API と「使い方」モード（v0.30.0）

他の拠点・他システムとのデータのやり取りは、v0.30.0 で「API」を中心にした形に変わりました。

- **読み取り API**（[API 画面](screen-readapi.html)）：この機に貯まったデータを、社内 LAN に常時つながる読み取り専用の API として公開できます。稼働の ON/OFF、トークンの発行・編集・削除、ブラウザで試せる **Swagger UI**（ベースURLの `/docs`）、curl のサンプルをこの画面で扱います。
- **API ソース**（[現場蓄積データ](screen-gemba.html)画面）：逆に、他の機・他システムが公開した読み取り API を「ソース」として登録すると、そのデータをこのアプリから検索できます。**すべてのソース**を選ぶと、登録した複数のソースのデータセット一覧を1画面にまとめて見られます（1クエリでの横断検索ではなく、在庫の一覧＋そこからの絞り込みです）。検索結果からは**成果物（添付ファイルなど）のダウンロード**もでき、現場 AI チャットも登録したソースへ向けて質問できます。
- **「使い方」（読み手 / 書き手）モード**：設定の「使い方」で、**ためる側（書き手）**と**読む側（読み手）**を切り替えられます。読む側を選ぶと、接続先・フロー・スケジュールなどの「システム」節が隠れ、現場 AI チャットと蓄積データの画面だけが残ります。切り替えは表示だけで、アクセス権限を制限するものではありません。
- **従来の共有**（チケット発行・[共有ビュー](screen-share.html)）は既定で非表示になりました。設定の「共有」欄でオンにすれば、これまでどおり使えます（拠点外・オフラインの相手など、API が使えない場合向け）。

## 表示バッジの意味と対処

画面に出る小さなバッジ（しるし）の意味と、消し方です。どちらも**実行には支障ありません**。

<a id="version-changed"></a>

### 版変更バッジ

フロー作成画面で、抽出に使うプラグインが**フロー作成時の版から新しい版に更新された**ときに出るお知らせです（例：`版変更 v1.2→v1.3`）。**配信はそのまま正常に動きます。**

- 消し方：そのフローを**開いて保存し直す**と、記録している版が現行に更新されて消えます。バッジ横の「**解消**」ボタンでも同じことができます（配信内容は変わりません）。
- 再実行では消えません（実行は記録版ではなく現行版で動くため、表示だけのお知らせです）。

<a id="unknown-provenance"></a>

### 出所不明 / 紐付け

「帳票ライブビュー」で、**接続先（i-Reporter サーバー）の識別が無い古い配信実績**に付くしるしです。接続先を切り替えた後などに、どのサーバー由来か判断できない記録を、誤って別サーバーの記録として扱わないために区別しています。

- **「未書き出し（実績なし）」との違い**：出所不明は「**配信実績はあるが出所が不明**」、未書き出しは「**そもそも配信実績が無い**」です。
- 対処：今の接続先のものだと分かっていれば、右の「**紐付け**」で現在の接続先の記録として確定できます。確定後はこの画面でのソフトデリート対象に含められます（別サーバー由来の記録は変更されません）。
